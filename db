CREATE DATABASE workshop;
use workshop;

CREATE TABLE clients(
    -> id INT AUTO_INCREMENT PRIMARY KEY,
    -> name VARCHAR(100) NOT NULL,
    -> address VARCHAR(255) NOT NULL,
    -> phone VARCHAR(20) NOT NULL UNIQUE
    -> );

CREATE TABLE cars (
    -> id INT AUTO_INCREMENT PRIMARY KEY, 
    -> client_id INT NOT NULL,
    -> license_number VARCHAR(50) NOT NULL UNIQUE,
    -> engine_number VARCHAR(50) NOT NULL UNIQUE,
    -> FOREIGN KEY (client_id) REFERENCES clients(id)
);

CREATE TABLE mechanics (
    -> id INT AUTO_INCREMENT PRIMARY KEY,
    -> name VARCHAR(100) NOT NULL,
    -> max_slots INT DEFAULT 4);

INSERT INTO mechanics (name) VALUES
    -> ('Jamal Bhuiya - M1'),
    -> ('Karim Uddin - M2'),
    -> ('Rafique Islam - M3'),
    -> ('Sohag Mia - M4');

CREATE TABLE appointments (
    -> id INT AUTO_INCREMENT PRIMARY KEY,
    -> car_id INT NOT NULL,
    -> mechanic_id INT NOT NULL,
    -> appointment_date DATE NOT NULL,
    -> status VARCHAR(20) DEFAULT 'Active',
    -> FOREIGN KEY (car_id) REFERENCES cars(id),
    -> FOREIGN KEY (mechanic_id) REFERENCES mechanics(id),
    -> UNIQUE (car_id, appointment_date));

CREATE TABLE admin (
    -> id INT AUTO_INCREMENT PRIMARY KEY,
    -> username VARCHAR(50) NOT NULL UNIQUE,
    -> password VARCHAR(255) NOT NULL
    -> );

INSERT INTO admin (username, password)
    -> VALUES ('admin', 'admin123');
INSERT INTO mechanics (name) VALUES ('Sohel Rana - M5');

CREATE TABLE mechanic_day_overrides (
    id INT AUTO_INCREMENT PRIMARY KEY,
    mechanic_id INT NOT NULL,
    override_date DATE NOT NULL,
    max_slots INT NOT NULL,
    UNIQUE (mechanic_id, override_date),
    FOREIGN KEY (mechanic_id) REFERENCES mechanics(id)
);
